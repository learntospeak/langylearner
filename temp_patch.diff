*** Begin Patch
*** Update File: lesson.html
@@
-                  const THREE = window.__THREE||window.THREE; const GLTFLoader = window.__GLTFLoader;
+                  const THREE = window.__THREE||window.THREE; const GLTFLoader = window.__GLTFLoader;
+                  // Read the same preview settings used by Anchor Tool / Shop
+                  const BASEMESH_SCALE = (function(){ try{ const v=parseFloat(localStorage.getItem('baseMeshScale')||'0.92'); return (isFinite(v)&&v>0)?v:0.92; } catch{ return 0.92; } })();
+                  const BOOT_INFLATE = (function(){ try{ const v=parseFloat(localStorage.getItem('bootInflate')||'0.009'); return (isFinite(v)&&v>=0)?v:0.009; } catch{ return 0.009; } })();
+                  function isBootish(id, slot){ try { return slot==='boots'||slot==='feet'||/shoe|bottes/i.test(String(id||'')); } catch { return false; } }
+                  function inflateScene(root, amount){ try { if(!root||!amount||!isFinite(amount)||amount<=0) return; root.traverse(o=>{ try{ if(!o||!o.isMesh||!o.geometry) return; const g0=o.geometry; const g=g0.clone(); o.geometry=g; if(!g.attributes||!g.attributes.position) return; if(!g.attributes.normal){ try{ g.computeVertexNormals(); }catch{} } const pos=g.attributes.position, nor=g.attributes.normal; if(!pos||!nor) return; const pa=pos.array, na=nor.array; const len=Math.min(pa.length, na.length); for(let i=0;i<len;i+=3){ pa[i]+=(na[i]||0)*amount; pa[i+1]+=(na[i+1]||0)*amount; pa[i+2]+=(na[i+2]||0)*amount; } pos.needsUpdate=true; try{ g.computeBoundingBox(); g.computeBoundingSphere(); }catch{} }catch{} }); } catch{} }
@@
-                  const gltfBase = await new Promise(res=>{ try{ loader.load(modelItem.model, g=>res(g), undefined, ()=>res(null)); } catch { res(null); } }); if (!gltfBase||!gltfBase.scene) { console.warn('[lesson-lv] base load failed'); return; }
+                  const gltfBase = await new Promise(res=>{ try{ loader.load(modelItem.model, g=>res(g), undefined, ()=>res(null)); } catch { res(null); } }); if (!gltfBase||!gltfBase.scene) { console.warn('[lesson-lv] base load failed'); return; }
@@
-                  const base = gltfBase.scene; const rootG = new THREE.Group(); scene.add(rootG); rootG.add(base);
+                  const base = gltfBase.scene; const rootG = new THREE.Group(); scene.add(rootG); rootG.add(base);
+                  try { if (String(modelItem.id||'')==='model-basemesh') { const k=BASEMESH_SCALE; if (isFinite(k)&&k>0) base.scale.set(k,k,k); } } catch{}
@@
-                  for (const slot of slots){ const v=eq[slot]; const ids=Array.isArray(v)?v:[v]; for (const id of ids){ const g=loaded.get(id); if(!g) continue; const obj=g.scene.clone(true); try{ const b=new THREE.Box3().setFromObject(obj); const c=b.getCenter(new THREE.Vector3()); obj.position.sub(c);}catch{} const wrapG=new THREE.Group(); wrapG.add(obj); const a=getAnchor(slot,id); try{ const boxBase=new THREE.Box3().setFromObject(base); const boxObj=new THREE.Box3().setFromObject(obj); const baseH=Math.max(1e-6, boxBase.max.y-boxBase.min.y); const objH=Math.max(1e-6, boxObj.max.y-boxObj.min.y); let target=(slot==='top'||slot==='torso'||slot==='body')?0.35:((slot==='boots'||slot==='feet')?0.12:0.25); let s=(a.scale&&a.scale[0])?a.scale[0]:((target*baseH)/objH); if(!(s>0&&s<10)) s=target; wrapG.scale.set(s,s,s);}catch{} try{ wrapG.position.set(a.position?.[0]||0, a.position?.[1]||0.05, a.position?.[2]||0);}catch{} try{ wrapG.rotation.order='XYZ'; wrapG.rotation.set(a.rotation?.[0]||0, a.rotation?.[1]||0, a.rotation?.[2]||0);}catch{} scene.add(wrapG); } }
+                  for (const slot of slots){ const v=eq[slot]; const ids=Array.isArray(v)?v:[v]; for (const id of ids){ const g=loaded.get(id); if(!g) continue; const obj=g.scene.clone(true); try{ const b=new THREE.Box3().setFromObject(obj); const c=b.getCenter(new THREE.Vector3()); obj.position.sub(c);}catch{} try{ let infl=0; if (String(id||'').toLowerCase()==='cos-bottesgreen') infl=Math.max(infl, BOOT_INFLATE*1.5); if (isBootish(id,slot)) infl=Math.max(infl, BOOT_INFLATE); if (infl>0) inflateScene(obj, infl);}catch{} const wrapG=new THREE.Group(); wrapG.add(obj); const a=getAnchor(slot,id); try{ let s=(a.scale&&a.scale[0])?a.scale[0]:1.0; wrapG.scale.set(s,s,s);}catch{} try{ wrapG.position.set(a.position?.[0]||0, a.position?.[1]||0.05, a.position?.[2]||0);}catch{} try{ wrapG.rotation.order='XYZ'; wrapG.rotation.set(a.rotation?.[0]||0, a.rotation?.[1]||0, a.rotation?.[2]||0);}catch{} scene.add(wrapG); } }
@@
-                    const base = gltf.scene; scene.add(base);
+                    const base = gltf.scene; scene.add(base); try { if (String(it.id||'')==='model-basemesh') { const k=BASEMESH_SCALE; if (isFinite(k)&&k>0) base.scale.set(k,k,k); } } catch{}
@@
-                    async function addOne(slot, id){ const it2 = items.find(x=>x.id===id); if(!it2||!it2.model) return; const gltf2 = await new Promise(res=>{ try{ loader.load(it2.model,g=>res(g),undefined,()=>res(null)); }catch{ res(null);} }); if(!gltf2||!gltf2.scene) return; const obj = gltf2.scene.clone(true); try{ const b=new THREE.Box3().setFromObject(obj); const c=b.getCenter(new THREE.Vector3()); obj.position.sub(c);}catch{} const wrapG=new THREE.Group(); wrapG.add(obj); const ov = (function(){ try { return JSON.parse(localStorage.getItem('anchorOverrides')||'{}')||{}; } catch { return {}; } })(); let a = ov[id] || ov[slot] || anchors[slot] || anchors.misc || { position:[0,0.05,0], rotation:[0,0,0], scale:[1,1,1] }; try{ let s=(a.scale&&a.scale[0])?a.scale[0]:1.0; wrapG.scale.set(s,s,s);}catch{} try{ wrapG.position.set(a.position?.[0]||0, a.position?.[1]||0.05, a.position?.[2]||0);}catch{} try{ wrapG.rotation.order='XYZ'; wrapG.rotation.set(a.rotation?.[0]||0, a.rotation?.[1]||0, a.rotation?.[2]||0);}catch{} scene.add(wrapG);} 
+                    async function addOne(slot, id){ const it2 = items.find(x=>x.id===id); if(!it2||!it2.model) return; const gltf2 = await new Promise(res=>{ try{ loader.load(it2.model,g=>res(g),undefined,()=>res(null)); }catch{ res(null);} }); if(!gltf2||!gltf2.scene) return; const obj = gltf2.scene.clone(true); try{ const b=new THREE.Box3().setFromObject(obj); const c=b.getCenter(new THREE.Vector3()); obj.position.sub(c);}catch{} try{ let infl=0; if (String(id||'').toLowerCase()==='cos-bottesgreen') infl=Math.max(infl, BOOT_INFLATE*1.5); if (isBootish(id,slot)) infl=Math.max(infl, BOOT_INFLATE); if (infl>0) inflateScene(obj, infl);}catch{} const wrapG=new THREE.Group(); wrapG.add(obj); const ov = (function(){ try { return JSON.parse(localStorage.getItem('anchorOverrides')||'{}')||{}; } catch { return {}; } })(); let a = ov[id] || ov[slot] || anchors[slot] || anchors.misc || { position:[0,0.05,0], rotation:[0,0,0], scale:[1,1,1] }; try{ let s=(a.scale&&a.scale[0])?a.scale[0]:1.0; wrapG.scale.set(s,s,s);}catch{} try{ wrapG.position.set(a.position?.[0]||0, a.position?.[1]||0.05, a.position?.[2]||0);}catch{} try{ wrapG.rotation.order='XYZ'; wrapG.rotation.set(a.rotation?.[0]||0, a.rotation?.[1]||0, a.rotation?.[2]||0);}catch{} scene.add(wrapG);} 
*** End Patch
